import {palettes} from "./palettes"

export const components = {
  dark: {
    ui: {
      wrapper: {
        bg: palettes.color[1],
      },
    },
  },

  light: {
    ui: {
      wrapper: {
        bg: palettes.color[3]
      },
    },
  },
};
